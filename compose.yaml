services:
  berichtsheft:
    build: .
    container_name: berichtsheft
    ports:
      - "127.0.0.1:8001:8000"
    env_file: .env
    environment:
      TZ: Europe/Berlin
    volumes:
      - ./data:/data
    restart: unless-stopped
